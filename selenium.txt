from selenium import webdriver #WAITS#
from selenium.webdriver.common.by import By
from selenium.webdriver.support.ui import WebDriver Wait
from selenium, webdriver.support import expected conditions as EC
driver = webdriver:.Chrome()
driver.get(”https://www.coocle.com")
driver.implicitly_wait(10)
search_box = driver.find_element(By.NAME, "q")
search box,send keys("Pune'")
# CODE CONTINUED ON NEXT
#continue from previous page code #wait = WebDriverWait(driver, 15)search box explicit = wait.until(
EC.presence_of_element_located((By.NAME, "q'"))
selarch box esplicit.send keys("Maharashtra")
polling wait = WebDriverWait(driver, 20, 
poll frequency=3, ignored exceptions=/Exception])
search box_ polling = polling_wait.until(
EC.visibility_of clement located(By.NAME, "q"))
search_box _polling, send_keys("India")
driver.quit)

--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

from selenium import webdriver
from selenium.webdriver.common.by import By
from selenium.webdriver.common.keys import Keys
driver webdriver:Chrome0
driver.get("httns://www.cooale.com/")
single_webele = driver.find_ element(By.XPATH, "//textarea")multi_ webele = driver:find_elements(By.XPATH, "/textarea")print(type(single_webele))
print(type(multi_webele))

---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

from selenium import webdriver
from selenium.webdriver.common.by import By
from selenium.webdriver.common.keys import Keys
driver = webdriver:Chrome)
driver.get("https://www.eoocle.com/")
search txtbox =
driver:find element(By.XPATH 
1//textareal@name='q'I")
search txtbox.send keys( imcc')
imcc dropdown =
driver:find elements(By.XPATH, 
"//span/contains(normalize-space), 'imcc'))")

--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

from selenium import webdriver
from selenium.webdriver.common.by import By
import time
driver = webdriver.Chrome)
driver,eet("https://www.cooqle.com')
driver.switch to.new window("window")driver,qet(”httns://www.facebook.com”)driver.switch to.new window("tab")
driver,get(”httos://www.youtube.com”)
time.sleep(10)
driver.close0#Also try driver.quit0
